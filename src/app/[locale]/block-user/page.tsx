// Імпорти бібліотек
import React from 'react';

// Імпортуємо компонент BlockedUser
import BlockedUser from '@/components/BlockUser/BlockUser';

const BlockedUserPage = () => {
  return <BlockedUser />;
};

export default BlockedUserPage;
